---
space: S000136
property: P000051
value: true
---

For each $r\in\mathbb{R}$ consider the open set $U_r := \{f \in 2^\mathbb{R} \to 2\mid f(\{r\})=1\}$ of $X$. Since $U_r \cap M = \{x_r\}$, it follows that $M$ is {P52}.

Hence, if $A \subseteq X$ contains no elements of $X \setminus M$ we are done, otherwise $A$ already contains an isolated point by definition.
