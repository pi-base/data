---
uid: T000849
if:
  P000200: true
then:
  P000229: true
---

Fix a point $x \in X$. The path component of $X$ containing $x$ is a singleton, so $\pi_1(X, x)$ is trivial and thus we can take $U := X$.
